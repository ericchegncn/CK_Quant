# CK Quant 2026.8.8

## 服务器管理

- 从成交额交易对列表勾选交易对并保存时，会自动把首个 `VolumePairList` 切换为 `StaticPairList`。
- 自动写入所选白名单，同时保留后续的 `AgeFilter` 等过滤器。
- 已经使用 `StaticPairList` 的配置保持原有参数不变。

## FreqAI Docker 镜像

- 新增 `ericchenghz/ck-quant:2026.8.8-freqai`，包含 LightGBM、XGBoost、随机森林、DataSieve 和 TensorBoard 等常规 FreqAI 依赖。
- 新增 `ericchenghz/ck-quant:2026.8.8-freqai-rl`，额外包含 PyTorch、Stable-Baselines3 与强化学习模型。
- 同步发布 `stable-freqai`、`freqai`、`stable-freqai-rl` 和 `freqai-rl` 标签。
- 标准镜像继续发布为 `2026.8.8`、`stable` 和 `latest`。

## Android APK

- `CK-Quant-Android-0.1.8-debug.apk`（versionCode 9）。
- APK 与 WebUI 使用同一套服务器管理修复。

## 隐私保护

- 发布源码、APK 和全部 Docker 镜像均不包含私有策略、实盘配置、API 密钥、Telegram Token、服务器 IP、数据库或模型数据。
